<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pay">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="adjustPan|stateHidden">
        <activity android:name=".GridViewPerformanceActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PerformanceActiity">

        </activity>
        <activity android:name=".PerspectivesActivity" />
        <activity android:name=".HouseRentActivity" />
        <activity android:name=".AnnualLeaveActivity" />
        <activity android:name=".SelectOfficerActivity" />
        <activity android:name=".ReasonLeaveActivity" />
        <activity android:name=".AppyForLeaveActivity"></activity>
        <activity android:name=".AnnouncementActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>